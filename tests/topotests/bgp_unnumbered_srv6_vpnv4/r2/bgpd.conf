log stdout notifications
log monitor notifications
log commands
log file frr.log debugging
!
debug bgp vpn label
debug bgp vpn leak-from-vrf
debug bgp vpn leak-to-vrf
debug bgp vpn rmap-event
!
router bgp 2
 bgp router-id 10.255.0.2
 no bgp default ipv4-unicast
 bgp bestpath as-path multipath-relax
 neighbor FAB peer-group
 neighbor FAB remote-as external
 neighbor FAB capability extended-nexthop
 neighbor r2-eth0 interface peer-group FAB
 segment-routing srv6
  locator default
 !
 address-family ipv6 unicast
  neighbor FAB activate
  network 2001:db8:2:2::/64
 exit-address-family
 !
 address-family ipv4 vpn
  neighbor FAB activate
 exit-address-family
!
router bgp 2 vrf vrf1
 bgp router-id 10.255.0.2
 !
 address-family ipv4 unicast
  redistribute connected
  rd vpn export 2:1
  rt vpn both 1:1
  sid vpn export locator explicit 2001:db8:2:2:fc3::
  export vpn
  import vpn
 exit-address-family
!
router bgp 2 vrf vrf2
 bgp router-id 10.255.0.2
 !
 address-family ipv4 unicast
  redistribute connected
  rd vpn export 2:2
  rt vpn both 2:2
  sid vpn export locator explicit 2001:db8:2:2:fc4::
  export vpn
  import vpn
 exit-address-family
!
